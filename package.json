{
  "name": "sweetalert2-esbuild-demo",
  "version": "1.0.0",
  "description": "Demo of using SweetAlert2 with esbuild",
  "author": "Limon Monte <limon.monte@gmail.com> (https://limonte.github.io)",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.8.31"
  },
  "dependencies": {
    "react": "^17.0.0",
    "react-dom": "^17.0.0",
    "sweetalert2": "^10.0.0",
    "sweetalert2-react-content": "^3.3.1"
  },
  "scripts": {
    "build": "esbuild app.jsx --bundle --outfile=bundle.js --define:process.env.NODE_ENV=\"\\\"production\\\"\""
  }
}
